import type { MongoCredentials } from '../mongo_credentials';
import { AzureTokenCache } from './azure_token_cache';
import { ServiceWorkflow } from './service_workflow';
/**
 * The Azure access token format.
 * @internal
 */
export interface AzureAccessToken {
    access_token: string;
    expires_in: number;
}
/**
 * Device workflow implementation for Azure.
 *
 * @internal
 */
export declare class AzureServiceWorkflow extends ServiceWorkflow {
    cache: AzureTokenCache;
    /**
     * Get the token from the environment.
     */
    getToken(credentials?: MongoCredentials): Promise<string>;
}
//# sourceMappingURL=azure_service_workflow.d.ts.map